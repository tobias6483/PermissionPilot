# v0.1.0-alpha.2 Release Notes

PermissionPilot v0.1.0-alpha.2 is a source-only follow-up alpha for the macOS permissions dashboard. It refines TCC permission-state semantics and dashboard copy after the first public alpha.

This prerelease is source-only on GitHub. Signed and notarized app artifacts are intentionally not attached yet; build locally from source while the distribution pipeline is finalized.

## Highlights

- Native SwiftUI macOS app.
- Clickable dashboard navigation for permissions, scan summary apps, installed apps, and background items.
- Code signing identity context for installed apps.
- Permission overview with sensitivity labels and grouped granted, denied, unknown, unavailable, and not-recorded evidence.
- Best-effort TCC record matching for known macOS permission services, including typed evidence for unreadable databases, missing records, query failures, Apple Events targets, and legacy authorization columns.
- Clear distinction between unavailable TCC data, readable-but-not-recorded permissions, and matched records with unknown authorization values.
- Selected-app permission details collapse all-not-recorded states into one neutral empty state instead of repeating rows.
- App list access badges describe permission sensitivity, not whether an app is legitimate or suspicious.
- A single prominent Full Disk Access action when macOS limits local TCC visibility.
- Developer Mode for local System Settings link QA during release checks.
- Conservative review-priority signals without malware verdicts.
- Educational explanations.
- System Settings shortcuts.
- First-run and empty-state guidance for limited macOS visibility.
- LaunchAgent, LaunchDaemon, login item, background task, ServiceManagement record, and privileged helper tool scanning.
- Compact background item review with kind summaries, stale/OK signals, stale reasons, and source evidence.
- Full and filtered local Markdown and JSON report export.

## Known Limitations

- No signed or notarized `.app` download is attached to this alpha release.
- macOS may prevent direct inspection of some permission states.
- Permission states may be shown as unavailable when macOS blocks access to TCC data, not recorded when readable TCC data has no matching record, or unknown when a matching record has an unrecognized authorization value.
- If TCC evidence is fully unavailable for a selected app, the app shows one limited-evidence notice instead of per-permission guesses.
- Login item and background task data depends on current `sfltool dumpbtm` output.
- Privileged helper stale detection is a heuristic review signal.
- Helper-tool detection may require conservative wording to avoid false positives.
